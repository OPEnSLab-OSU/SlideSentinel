# include_directories("${CMAKE_CURRENT_LIST_DIR}/test/test_common")
FILE(GLOB_RECURSE SRC_LIST ${SRC_LIST} "${CMAKE_CURRENT_LIST_DIR}/test/*.*")
# add_definitions(-D'UNIT_TEST')
add_definitions(-D'ARDUINO')
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -frtti")